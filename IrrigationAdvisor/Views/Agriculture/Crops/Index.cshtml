@model IEnumerable<IrrigationAdvisor.Models.Agriculture.Crop>

@{
    ViewBag.Title = "Index";
    Layout = "Agriculture";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Region.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Specie.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Density)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MaxEvapotranspirationToIrrigate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MinEvapotranspirationToIrrigate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Region.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Specie.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Density)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MaxEvapotranspirationToIrrigate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MinEvapotranspirationToIrrigate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CropId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CropId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CropId })
        </td>
    </tr>
}

</table>
