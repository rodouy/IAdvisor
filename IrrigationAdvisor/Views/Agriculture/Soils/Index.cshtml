@model IEnumerable<IrrigationAdvisor.Models.Agriculture.Soil>

@{
    ViewBag.Title = "Index";
    Layout = "Agriculture";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TestDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DepthLimit)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TestDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DepthLimit)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SoilId }) |
            @Html.ActionLink("Details", "Details", new { id=item.SoilId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SoilId })
        </td>
    </tr>
}

</table>
