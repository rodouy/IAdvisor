@model IEnumerable<IrrigationAdvisor.Models.Management.Message>

@{
    ViewBag.Title = "Index";
    Layout = "Management";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Title.TitleId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CropIrrigationWeatherId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Title.TitleId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CropIrrigationWeatherId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MessageId }) |
            @Html.ActionLink("Details", "Details", new { id=item.MessageId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MessageId })
        </td>
    </tr>
}

</table>
